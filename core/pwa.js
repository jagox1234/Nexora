// PWA (instalable, manifest, service worker)
export function registerServiceWorker() {
  // TODO: registrar service worker
  return { ok: true };
}

export function updateManifest(manifest) {
  // TODO: actualizar manifest
  return { ok: true, manifest };
}
