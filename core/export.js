// Exportación (CSV/Excel) de reservas/clientes/servicios
export function exportToCSV(data, filename) {
  // TODO: convertir data a CSV y descargar
  return { ok: true, filename };
}

export function exportToExcel(data, filename) {
  // TODO: convertir data a Excel y descargar
  return { ok: true, filename };
}
