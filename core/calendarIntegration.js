// Integración con calendario externo (Google/Outlook)
export function addToCalendar({ bookingId, calendarType, details }) {
  // TODO: integración real con API de calendario
  return { ok: true, bookingId, calendarType };
}
