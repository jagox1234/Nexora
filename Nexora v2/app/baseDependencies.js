// v2/app/baseDependencies.js — migrated from legacy Nexora/2_dependencies.js
import AsyncStorage from '@react-native-async-storage/async-storage';
import { LinearGradient } from 'expo-linear-gradient';
import React, { useState, useEffect, useMemo, useCallback, useRef, useContext, createContext } from 'react';
import { View, Text, ScrollView, FlatList, TouchableOpacity, Pressable, TextInput, StyleSheet, Image, SafeAreaView, ActivityIndicator, Alert, Platform, Modal, Animated, Easing, Linking } from 'react-native';
import { Svg, Path, Rect, Circle, Line, Polyline, Polygon } from 'react-native-svg';

export {
  React,
  useState,
  useEffect,
  useMemo,
  useCallback,
  useRef,
  useContext,
  createContext,
  View,
  Text,
  ScrollView,
  FlatList,
  TouchableOpacity,
  Pressable,
  TextInput,
  StyleSheet,
  Image,
  SafeAreaView,
  ActivityIndicator,
  Alert,
  Platform,
  Modal,
  Animated,
  Easing,
  AsyncStorage,
  LinearGradient,
  Svg,
  Path,
  Rect,
  Circle,
  Line,
  Polyline,
  Polygon
  , Linking
};
